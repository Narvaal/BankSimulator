{
  "name": "banksimulator-root",
  "private": true,
  "description": "Raiz do repositório — apenas tooling (Husky git hooks). Backend é Maven, frontend vive em frontend/assetstore.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}
