admin.trigger.token=${ADMIN_TRIGGER_TOKEN}
kofi.verification-token=${KOFI_VERIFICATION_TOKEN}
google.client-id=${GOOGLE_CLIENT_ID}
db.default.url=${DB_URL}
db.default.user=${DB_USER}
db.use.test=false
server.port=5000
server.address=0.0.0.0
db.test.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
db.test.user=sa
db.test.password=
aws.ses.from=${AWS_SES_FROM}
app.base-url=${APP_BASE_URL}
webhook.asset.enabled=true
webhook.asset.url=${WEBHOOK_ASSET_URL}
db.schema.auto-create=false
security.jwt.secret-key=${JWT_SECRET_KEY}
security.jwt.expiration-time=${JWT_EXPIRATION_TIME}
